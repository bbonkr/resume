/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    trailingSlash: false,
    images: {
        domains: ['storage.bbon.me'],
    },
};

module.exports = nextConfig;
